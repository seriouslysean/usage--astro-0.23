# Astro Usage (+ Vue 3)

Pushing to `main` will automatically deploy the changes to Vercel, https://astro-usage.vercel.app/.

To run a dev build:

```
npm run dev
```

To run a prod build:

```
npm run preview
```

